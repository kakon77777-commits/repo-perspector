# Benchmark Notes — v0.6.0

所有數據都是本次隔離執行環境中的單次樣本，只適合用來確認回歸與成本邊界，不應視為跨機器、跨語言或跨 Repository 的固定效能承諾。

## 1,200 個 Python 模組的合成倉庫

樣本由 1,200 個線性相依的 Python 模組組成，使用單 worker、關閉 Git 歷史。

| 模式 | 秒 |
|---|---:|
| 無快取 | 3.947744 |
| 建立快取 | 3.747158 |
| 暖快取，1,200／1,200 命中 | 3.868653 |

暖快取相對快取建立輪為 `0.969×`，也就是沒有加速，反而有輕微波動。這表示在此種大量小檔案、長依賴鏈樣本裡，解析不再是主要瓶頸；依賴解析、圖建構、傳遞影響、品質與風險計算佔據了較多時間。

因此：

- `cache_hit_rate = 100%` 不等於端到端一定更快。
- 快取對昂貴解析器、較大檔案或 I/O 受限環境可能更有價值。
- 後續大型倉庫優化應集中在增量圖更新，而不只是檔案解析快取。

原始數據：`benchmarks/v0.6-synthetic.json`。

## 持久化狀態與趨勢

以 v0.6 自我透視完整報告為基礎，建立 50 份 gzip 快照：

| 指標 | 結果 |
|---|---:|
| 50 份快照總寫入 | 1.932715 秒 |
| 平均每份快照 | 38.654 毫秒 |
| 讀取 manifest 並建立趨勢 | 0.000690 秒 |
| 生成 JSON／Markdown／HTML 趨勢 | 0.008566 秒 |
| 狀態庫總大小 | 2,635,873 bytes |

趨勢聚合只使用 `history.json` 的摘要，不必逐一解壓完整報告，因此快照數量增加時仍能保持低成本。完整 gzip 快照則保留給日後深度比較與稽核。

原始數據：`benchmarks/v0.6-state.json`。

## 自我透視暖快取

v0.6 對自身執行兩次分析，第二次結果：

```text
cache hits: 52
cache misses: 0
hit rate: 100%
```

這僅是小型專案回歸訊號，不代替大型、多語言實測。
